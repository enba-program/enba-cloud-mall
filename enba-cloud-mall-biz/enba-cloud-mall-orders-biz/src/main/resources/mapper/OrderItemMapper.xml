<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enba.cloud.orders.order.mapper.OrderItemMapper">

  <insert id="insertBatchSomeColumn">
    insert into order_item (id, order_id, product_id,
    product_name, product_image, current_price,
    quantity) values
    <foreach item="item" collection="list" separator="," open="(" close=")" index="index">
      (#{item.id}, #{item.orderId}, #{item.productId},
      #{item.productName}, #{item.productImage}, #{item.currentPrice},
      #{item.quantity})
    </foreach>
  </insert>
</mapper>
